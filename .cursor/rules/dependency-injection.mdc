---
description: Implement dependency injection to allow components to interact with different data sources or services.
globs: core/*
---
{
  "rule_name": "Dependency Injection",
  "implementation_guidelines": [
    "Use dependency injection to provide components with the necessary data sources or services. This makes it easier to test components and swap out dependencies. For example, a `BudgetReporter` class can accept a `data_source` object, allowing it to retrieve transactions from an API or a local database.",
    "python",
    "class BudgetReporter:",
    "def __init__(self, data_source):",
    "self.data_source = data_source",
    "def generate_report(self):",
    "return self.data_source.get_transactions()"
  ],
  "code_examples": [],
  "best_practices": [],
  "dependencies": []
}