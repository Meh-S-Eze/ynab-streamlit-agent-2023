---
description: Enable key features to be configured at runtime without requiring code changes or redeployment.
globs: core/config.py, *.env
rule_name: Support Runtime Configurable Features
---
{
  "implementation_guidelines": [
    "Use a configuration management library (e.g., `python-decouple`, `Dynaconf`).",
    "Load configuration settings from environment variables or configuration files.",
    "Implement a mechanism for reloading configuration settings without restarting the application."
  ],
  "code_example": "",
  "best_practices": [
    "Use a schema validation library (e.g., `Pydantic`) to validate configuration settings.",
    "Securely store sensitive configuration settings (e.g., API keys) using a secrets management solution."
  ],
  "dependencies": []
}